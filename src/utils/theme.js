import { backgroundColor, blue, textColor, titleColor } from "./colors"
import { primaryFont, typeScale } from "./typography"

export const defaultTheme = {
  primaryColor: blue,
  titleColor,
  textColor,
  backgroundColor,
  primaryFont,
  typeScale,
}
